% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{status}
\alias{status}
\title{Report project package status}
\usage{
status(
  path = ".",
  lockfile = NULL,
  project_file = file.path(".zak", "project.json"),
  lib = NULL,
  include_extra = TRUE
)
}
\arguments{
\item{path}{A single initialized project directory.}

\item{lockfile}{\code{NULL} to use the lockfile recorded in project metadata, or
a single lockfile name or path.}

\item{project_file}{A single project metadata name or path.}

\item{lib}{\code{NULL} to use the project's isolated library when configured, or
a single library path to inspect.}

\item{include_extra}{A single logical value. When \code{TRUE}, include installed
packages that are not selected by the lockfile.}
}
\value{
An object of class \code{zak_status}, invisibly, containing \code{path},
\code{lockfile}, \code{library}, and a \code{packages} data frame with \code{Package},
\code{Expected}, \code{Installed}, and \code{Status} columns.
}
\description{
\code{status()} compares the selected package versions in an initialized
project's lockfile with the packages installed in its project library. It
reports \code{ok}, \code{missing}, \code{changed}, and, by default, \code{extra} packages. The
operation is read-only and does not resolve or install packages.
}
\examples{
\dontrun{
project_status <- zak::status(".")
project_status$packages
}
}
\seealso{
\code{\link[=snapshot]{snapshot()}}, \code{\link[=restore]{restore()}}, \code{\link[=project_library]{project_library()}}
}
