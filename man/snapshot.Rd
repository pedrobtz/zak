% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{snapshot}
\alias{snapshot}
\title{Record the current project resolution}
\usage{
snapshot(
  path = ".",
  lockfile = NULL,
  project_file = file.path(".zak", "project.json"),
  lib = NULL,
  verbose = FALSE
)
}
\arguments{
\item{path}{A single initialized project directory.}

\item{lockfile}{\code{NULL} to use the lockfile recorded in project metadata, or
a single lockfile name or path.}

\item{project_file}{A single project metadata name or path.}

\item{lib}{\code{NULL} to use the project's isolated library when configured, or
a single library path used while resolving dependencies.}

\item{verbose}{A single logical value. When \code{TRUE}, report snapshot and
planning steps.}
}
\value{
An object of class \code{zak_project}, invisibly, containing the updated
lockfile, library path, discovered dependencies, and ready plan(s).
}
\description{
\code{snapshot()} re-scans an initialized project, resolves its current package
references, and replaces its lockfile without installing packages. The
project's isolated library is used while planning by default, so packages
already present there are recorded as available rather than selected for
installation again.
}
\examples{
\dontrun{
project <- zak::snapshot(".")
project$lockfile
}
}
\seealso{
\code{\link[=init]{init()}}, \code{\link[=status]{status()}}, \code{\link[=restore]{restore()}}
}
