% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/packages.R
\name{available_packages}
\alias{available_packages}
\title{Explore available packages}
\usage{
available_packages(repos = getOption("repos"), ...)
}
\arguments{
\item{repos}{A character vector of repository URLs. Defaults to
\code{getOption("repos")}.}

\item{...}{Additional arguments passed to \code{\link[utils:available.packages]{utils::available.packages()}},
such as \code{type} or \code{filters}.}
}
\value{
A data frame with one row per available package and the columns
returned by \code{\link[utils:available.packages]{utils::available.packages()}}. Column names are preserved,
every column is character, and automatic row names allow duplicate package
names. A zero-row matrix returned by R becomes a zero-row data frame with
the same columns.
}
\description{
\code{available_packages()} wraps \code{\link[utils:available.packages]{utils::available.packages()}} and returns its
package database as a data frame instead of a character matrix. Warnings and
errors from repository access are preserved.
}
\examples{
\dontrun{
packages <- available_packages()
packages[packages$Package == "jsonlite", c("Package", "Version")]
}
}
\seealso{
\code{\link[=installed_packages]{installed_packages()}}
}
