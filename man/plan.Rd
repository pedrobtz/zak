% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan.R
\name{plan}
\alias{plan}
\title{Create an installation plan for an R package}
\usage{
plan(pkg, lib = NULL, verbose = FALSE)
}
\arguments{
\item{pkg}{A single package reference. This can be a package name, a local
package directory, a local R package \code{.tar.gz} or \code{.zip} archive, or an
HTTP(S) URL to an R package \code{.tar.gz} or \code{.zip} archive. Git references
use \verb{git::URL[@ref]}; GitHub references use
\verb{github::owner/repository[@ref]}; Bioconductor references use
\code{bioc::package}.}

\item{lib}{\code{NULL}, or a single character path to the installation library.
Installed packages in this library are considered when resolving
dependencies.}

\item{verbose}{A single logical value. When \code{TRUE}, report each planning
step.}
}
\value{
An object of class \code{zak_plan} containing the source, acquisition,
platform, normalized target and dependency candidates, target and
dependency selections, constraints, repository metadata, and plan status.
When package metadata contains \verb{Remotes:}, normalized source mappings are
available in \verb{$remotes}.
}
\description{
\code{plan()} resolves an R package reference and returns the selected
installation plan. It does not install packages or mutate the target
library. The current references are package names from configured
repositories, local package directories, and HTTP(S) URLs to \code{.tar.gz} or
\code{.zip} archives, GitHub/Git references, and Bioconductor package
references.
}
\details{
Local \code{.tar.gz} and \code{.zip} archives are inspected in place and are never
removed by planning.

Git sources are cloned to a temporary checkout. Plans retain the requested
ref and resolved commit, but not the checkout path.

Plans record the selected target, dependency order, versions, repositories,
constraints, availability, source, acquisition metadata, and the current
platform facts. A dependency listed in \verb{Remotes:} is prepared from its
declared Git, URL, or Bioconductor source before resolution, so the plan
retains the source-specific candidate and provenance. URL archives are
downloaded to a temporary location so their metadata can be resolved. The
temporary path is not retained in the returned plan and is removed when
planning completes. Remote HTTP(S) archives and commit-resolved Git sources
reuse the cache configured by \code{options(zak.cache)} when available. Acquired
archives and Git trees record SHA-256 checksums; cache entries and target
installation are verified against those checksums.
Set \code{options(zak.offline = TRUE)} to require valid cached HTTP(S) archives
and commit-resolved Git sources during planning.

\verb{Remotes:} source identities must be inferable. Conflicting direct or
transitive declarations are rejected with their package, sources, and
declaring packages; failed remote preparation reports the dependency and
source context.
}
\examples{
\dontrun{
package_plan <- zak::plan("jsonlite")
package_plan$dependencies
}
}
\seealso{
\code{\link[=install]{install()}}
}
