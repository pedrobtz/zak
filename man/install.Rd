% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{install}
\alias{install}
\title{Install an R package with its dependencies}
\usage{
install(pkg, lib = NULL, verbose = FALSE)
}
\arguments{
\item{pkg}{A single package reference. This can be a package name, a local
package directory, a local R package \code{.tar.gz} or \code{.zip} archive, or an
HTTP(S) URL to an R package \code{.tar.gz} or \code{.zip} archive.
Git references use \verb{git::URL[@ref]}; GitHub references use
\verb{github::owner/repository[@ref]}.
Bioconductor references use \code{bioc::package}.}

\item{lib}{\code{NULL}, or a single character path to the installation library.
The default leaves library selection to \code{utils::install.packages()}.}

\item{verbose}{A single logical value. When \code{TRUE}, report each zak step,
including download, archive inspection, dependency resolution,
installation, and provenance recording. Output from R's package installer
is unchanged.}
}
\value{
The return value of the target \code{utils::install.packages()} call,
invisibly. That value is always \code{NULL}; a successful return, rather than
the value, is what signals that the library was committed.
}
\description{
\code{install()} resolves an R package reference, installs missing required
dependencies from configured repositories or their declared \verb{Remotes:}
sources, and then installs the requested package. References can be package
names, local package directories, local \code{.tar.gz} or \code{.zip} archives, or
HTTP(S) URLs to \code{.tar.gz} or \code{.zip} archives, Git/GitHub references, and
Bioconductor package references.
Source ZIP archives are extracted before installation. ZIP archives whose
\code{DESCRIPTION} contains a \code{Built} field are treated as Windows binary
packages and can be installed only on Windows.
}
\details{
For URL archives, the installed package records \code{RemoteType: url} and the
original URL in \code{RemoteUrl}, following the metadata convention used by the
remotes package. This allows renv to record the package with \code{Source: URL}
and restore it from the same archive. Local archives do not receive remote
URL metadata. Git sources also omit remote URL metadata in this iteration.
Because the full URL is retained in the installed package metadata and
lockfiles, it should not contain embedded credentials or other secrets.

Dependency handling follows \code{utils::install.packages(dependencies = NA)} for
repository-only plans: \code{Depends} (except \code{R}), \code{Imports}, and \code{LinkingTo}
are installed, while \code{Suggests} and \code{Enhances} are not. A matching
\verb{Remotes:} declaration selects and installs the remote source for that
dependency; its transitive dependencies are resolved in the same graph.
Dependency package types follow R's normal platform configuration. When
\code{lib = NULL}, the \code{lib} argument is omitted from \code{install.packages()} so R
performs its normal library selection.
During metadata inspection, \code{install()} reports a binary package when the
archive's \code{DESCRIPTION} contains \code{Built}, and a source package otherwise.
A non-empty \code{SystemRequirements} field is also reported without attempting
to check or install those system dependencies.
It then lists dependencies needing installation with their repository
versions and declared version constraints.
Dependencies and the target are installed into a temporary staging library
and committed to the requested library only after the complete staged set
has been verified. A failed dependency or target install therefore does not
commit packages from that operation, and \code{install()} raises an error naming
each package that is missing or built at an unexpected version. Note that
\code{utils::install.packages()} itself only warns on a failed build, so this
error is what distinguishes a failed install from a successful one.
Set \code{options(zak.offline = TRUE)} to require valid cached HTTP(S) archives
and commit-resolved Git sources during installation.
Other package validity and compatibility checks are left to R itself.

Apart from resolving the archive's dependencies, installation behavior,
conditions, validation, and return behavior are those of
\code{utils::install.packages()}.
}
\examples{
\dontrun{
zak::install("jsonlite")
zak::install("https://example.org/examplePackage_1.0.0.tar.gz")
zak::install("/path/to/local/package", verbose = TRUE)
}
}
\seealso{
\code{\link[=plan]{plan()}}
}
