% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lock.R
\name{compare_lock}
\alias{compare_lock}
\title{Compare a lockfile with an installation plan}
\usage{
compare_lock(lock, plan)
}
\arguments{
\item{lock}{A \code{zak_lock} returned by \code{\link[=read_lock]{read_lock()}}, or a path to a zak JSON
lockfile.}

\item{plan}{A ready object returned by \code{\link[=plan]{plan()}}.}
}
\value{
An object of class \code{zak_lock_comparison} with \code{status} equal to
\code{"current"} or \code{"drifted"}, and a \code{changes} data frame containing one
row per changed decision.
}
\description{
\code{compare_lock()} compares stable resolution decisions in a validated
lockfile with a newly generated \code{\link[=plan]{plan()}}. It is read-only: no packages are
installed and no lockfile is changed. Acquisition timestamps and methods
are intentionally ignored because they describe when and how an archive
was fetched rather than which artifact was selected.
}
\examples{
\dontrun{
package_plan <- zak::plan("jsonlite")
package_lock <- zak::read_lock("zak.lock")
zak::compare_lock(package_lock, package_plan)
}
}
\seealso{
\code{\link[=read_lock]{read_lock()}}, \code{\link[=plan]{plan()}}
}
