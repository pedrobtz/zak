% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{cache_clean}
\alias{cache_clean}
\title{Remove entries from the zak artifact cache}
\usage{
cache_clean(path = NULL, max_age = Inf, dry_run = FALSE)
}
\arguments{
\item{path}{\code{NULL}, or a cache directory to clean. The default uses the
directory configured by \code{options(zak.cache)} or R's user cache directory.}

\item{max_age}{A non-negative number of seconds. Entries modified more
recently than this age are retained. \code{Inf} removes all entries.}

\item{dry_run}{A single logical value. When \code{TRUE}, report selected entries
without deleting them.}
}
\value{
A data frame describing the selected entries.
}
\description{
\code{cache_clean()} removes cached HTTP(S) archives and commit-resolved Git
source trees. By default it removes all entries in the selected cache. Set
\code{max_age} to retain entries modified within the given number of seconds.
Cache reads do not update that timestamp, so it reflects when an entry was
stored rather than when it was last used. The returned data frame describes
entries selected for removal; use \code{dry_run = TRUE} to preview the selection
without changing the cache.
}
\examples{
\dontrun{
zak::cache_clean(max_age = 30 * 24 * 60 * 60, dry_run = TRUE)
zak::cache_clean(max_age = 30 * 24 * 60 * 60)
}
}
