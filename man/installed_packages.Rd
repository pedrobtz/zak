% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/packages.R
\name{installed_packages}
\alias{installed_packages}
\title{Explore installed packages}
\usage{
installed_packages(lib.loc = NULL, ...)
}
\arguments{
\item{lib.loc}{A character vector of library trees to search, or \code{NULL} to
use all known libraries.}

\item{...}{Additional arguments passed to \code{\link[utils:installed.packages]{utils::installed.packages()}},
such as \code{priority} or \code{fields}.}
}
\value{
A data frame with one row per installed package and the columns
returned by \code{\link[utils:installed.packages]{utils::installed.packages()}}. Column names are preserved and
every column is character. Automatic row names allow the same package to
appear in multiple libraries.
}
\description{
\code{installed_packages()} wraps \code{\link[utils:installed.packages]{utils::installed.packages()}} and returns its
package database as a data frame instead of a character matrix. A package
found in multiple libraries has one row per library, distinguished by
\code{LibPath}.
}
\examples{
packages <- installed_packages(priority = "base")
packages[, c("Package", "Version")]
}
\seealso{
\code{\link[=available_packages]{available_packages()}}
}
