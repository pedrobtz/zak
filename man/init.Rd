% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{init}
\alias{init}
\title{Initialize a project}
\usage{
init(
  path = ".",
  lockfile = "zak.lock",
  project_file = file.path(".zak", "project.json"),
  lib = NULL,
  verbose = FALSE,
  force = FALSE,
  isolated = TRUE
)
}
\arguments{
\item{path}{A single project directory.}

\item{lockfile}{A single lockfile name or path. Relative paths are resolved
from \code{path} and parent directories are created as needed.}

\item{project_file}{A single project metadata name or path. Relative paths
are resolved from \code{path} and parent directories are created as needed.}

\item{lib}{\code{NULL}, or a single character path to the installation library
used while resolving dependencies.}

\item{verbose}{A single logical value. When \code{TRUE}, report initialization
and planning steps.}

\item{force}{A single logical value. When \code{TRUE}, replace existing project
metadata and an existing lockfile. The default refuses to overwrite
either.}

\item{isolated}{A single logical value. When \code{TRUE}, create the project's
\code{.zak/library} and record it as the default project library. When \code{FALSE},
do not create or use an isolated library.}
}
\value{
An object of class \code{zak_project}, invisibly, containing the
expanded project path, project metadata path, lockfile path, isolated
library path (or \code{NULL}), discovered dependencies, and ready installation
plan(s).
}
\description{
\code{init()} discovers the project's static dependencies and records them in
\code{.zak/project.json}. For a local R package project it resolves the package
and its required dependencies through \code{\link[=plan]{plan()}} and writes the resulting
\code{zak.lock} through \code{\link[=lock]{lock()}}. For a non-package project it resolves each
discovered package reference and writes a project-level lockfile. By
default it also creates an empty isolated \code{.zak/library}; it does not
install packages or activate the project library.
}
\details{
The project lockfile contains one embedded package plan per root reference.
\code{restore()} validates every embedded plan, aggregates their selected
packages, and commits them in one staged installation transaction. Isolated
project libraries and activation remain future extensions.
}
\examples{
\dontrun{
project <- zak::init(".")
project$lockfile
}
}
\seealso{
\code{\link[=dependencies]{dependencies()}}, \code{\link[=plan]{plan()}}, \code{\link[=lock]{lock()}}
}
