# unknown package references fail before installation

    Code
      zak::plan("zakfixturemissingpackage", lib = library)
    Condition
      Error:
      ! Package 'zakfixturemissingpackage' is not available in the configured repositories.

